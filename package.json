{
  "name": "herdr-plugin-github-start",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "check": "node --check open.js && node --check prompt.js && node --check launcher.js && node --check session-discovery.js && node --check checkout-routing.js && node --test"
  }
}
